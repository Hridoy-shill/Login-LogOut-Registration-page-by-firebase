import React, { useContext } from 'react';
import { UserContext } from './AuthProviders';
import { Navigate } from 'react-router-dom';

const Profile = () => {
    
    return (
        <div>
            <p>this is the use profile!!</p>
        </div>
    );
};

export default Profile;